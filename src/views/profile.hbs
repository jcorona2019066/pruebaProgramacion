{{!-- Vista del usuario que inicio sesion  --}}
<body class="fondo2">
<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card-header ">
          <h1 class="text-white text-center bg-primary">Perfil</h1>
        </div>
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido </h3>
                    <h4>{{user.username}}</h4>
                    <h4>{{user.dpi}}</h4>
                    <h6>{{user.correoUsuario}}</h6>
                    
                </div>
            </div>
        </div>

    </div>
</div>
</body>